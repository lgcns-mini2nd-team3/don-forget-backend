spring:
  application:
    name: notification-service

  cloud:
    config:
      uri: http://localhost:8888
      enabled: true

  profiles:
    active: local

  config:
    import: "optional:configserver:"
    fail-fast: true
