server:
  port: 8085 # 임의 설정

spring:
  application:
    name: notification-service

  # 데이터베이스 연결 설정
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/don-forget
    username: root # 본인의 DB 아이디
    password: sohyun0524! # 본인의 DB 비밀번호

  # JPA 설정
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# 이메일 설정 (추후 구현시)
# mail:
#   host: smtp.gmail.com
