server:
  port: 8080

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mariadb://localhost:3306/team3
    username: root
    password: myky0226@
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: create
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

jwt:
  secret: daniel-secret-key-123456789